# ============================================================
# Steel Detailing DMS — Backend Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ============================================================

# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/steel_dms

# JWT secret — use a long random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_USE_A_LONG_RANDOM_SECRET_HERE

# JWT token expiry
JWT_EXPIRES_IN=8h

# Server port
PORT=5000

# Node environment
NODE_ENV=development

# Allowed frontend origin (for CORS)
CORS_ORIGIN=http://localhost:5173

# External App A APIs for Project Management Integration
APP_A_API_URL=http://localhost:5001/api/projects/count/total
APP_A_PROJECTS_URL=http://localhost:5001/api/projects
APP_A_API_KEY=test_api_key_for_external_application_access

