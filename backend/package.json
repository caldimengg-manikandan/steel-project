{
  "name": "steel-dms-backend",
  "version": "2.0.0",
  "description": "Steel Detailing Document Management System — Python/FastAPI Backend",
  "note": "This project now runs on FastAPI (Python). The npm scripts below are just aliases for convenience.",
  "scripts": {
    "start": "python -m uvicorn main:app --port 5000",
    "dev": "python -m uvicorn main:app --port 5000 --reload"
  }
}
